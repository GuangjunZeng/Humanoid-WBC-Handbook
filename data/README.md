# Canonical handbook data

Place reviewed source records in `sources/` and Engineering Claims in `claims/`. The initial repository intentionally contains no real engineering guidance: content must be researched and reviewed rather than fabricated for a demo.

Use `examples/manual-source.json` to exercise the importer. Synthetic acceptance fixtures live in the tests and are never indexed as handbook advice.
