# HOVER：把多种全身控制接口蒸馏进一个可切换策略

来源：[arXiv:2410.21229v2](https://arxiv.org/abs/2410.21229v2) · [版本固定的官方代码](https://github.com/NVlabs/HOVER/tree/8088f6cfb42a8f307dc614735197796a86ce8490)

解读范围：完整 8 页论文；本文只保存原创分析、定位信息和链接，不保存论文全文或代码。

## 工程痛点

导航常给根速度，遥操作常给头手关键点，操作策略又可能给上肢关节角。若每种命令接口都训练一个低层策略，系统会产生策略切换、重复训练和接口碎片。HOVER 试图把这些接口表示成同一命令向量的不同掩码，再让一个策略覆盖它们。

## 方法主线：输入 → 处理 → 输出

1. 将 AMASS 重定向到 19-DoF Unitree H1，并过滤为机器人可跟踪运动集 `Q̂`。
2. 用全刚体姿态、速度和参考运动训练一个特权全身模仿教师；教师是 `[512,256,128]` 的三层 MLP。
3. 把命令统一成三组原子量：关键刚体三维位置、局部关节角，以及根速度/高度/滚转俯仰偏航。
4. 上下半身分别选择模式，再用稀疏掩码丢弃部分元素。每个二值位来自 `Bernoulli(0.5)`，训练时每回合采样一次并保持到回合结束。
5. 学生接收 25 步关节、本体姿态、重力和动作历史；学生 rollout 到自己的状态，教师在对应特权状态给动作标签，学生最小化动作差。
6. 策略输出 19 个关节目标角，由 PD 控制器执行。部署时上层只需改变掩码和对应目标，不必更换低层网络。

关键蒸馏目标为 `L = ||â_t - a_t||²₂`。命令可写成 `M_sparsity ⊙ [M_mode ⊙ s_upper, M_mode ⊙ s_lower]`；它表达的是接口选择，不是安全约束。

## 关键图表怎么读

- Figure 1：三类原子命令及掩码构成统一接口；它解释了 H2O、OmniH2O、ExBody、HumanPlus 等模式怎样成为同一空间的子集。
- Figure 2：教师看完整全身状态，学生看历史与被掩码命令；这是“先学完整动作先验，再压缩为多模式策略”的核心。
- Table III：在重定向数据集、5 个随机种子上，HOVER 在每种既有模式至少赢 12 项指标中的 7 项；OmniH2O 模式的全局/局部位置误差为 128/62.5 mm，对照专用策略为 149/76.4 mm。
- Table IV：左手、右手、双手、头部四种稀疏模式下，HOVER 在被该模式实际跟踪的指标上整体优于分别训练的专用策略。
- Figure 4：同样随机掩码的端到端多模式 RL 基线在 8 个模式 × 4 个指标上均落后于教师蒸馏，说明收益不能只归因于命令掩码。
- Table V：20 条实机站立动作、5 次测试中，HOVER 在 12 个比较项里有 11 项更好；但这不是动态行走的大样本定量验证。

## 最有说服力的实验

Figure 4 最能隔离机制：HOVER 与多模式 RL 使用相同掩码训练流程，主要差别是是否由完整运动模仿教师蒸馏。HOVER 在 32/32 个“模式—指标”组合上误差更低，支持“完整运动先验可帮助多模式策略”的窄结论。它仍只在同一仿真、数据、奖励和机器人族内比较，不能证明蒸馏对任意命令空间都优于 RL。

## 论文—代码映射

| 论文组件 | 代码位置 | 对应关系 |
|---|---|---|
| 模式与稀疏掩码 | [`create_mask`](https://github.com/NVlabs/HOVER/blob/8088f6cfb42a8f307dc614735197796a86ce8490/neural_wbc/core/neural_wbc/core/mask.py) | 按模式启用命令元素，并可对已启用元素执行 0.5 概率的 Bernoulli 稀疏化 |
| 命令/掩码维数 | [`calculate_mask_length` / `calculate_command_length`](https://github.com/NVlabs/HOVER/blob/8088f6cfb42a8f307dc614735197796a86ce8490/neural_wbc/core/neural_wbc/core/mask.py) | 区分每个刚体一个掩码位与每个刚体三维位置命令，避免接口维数混淆 |
| DAgger rollout 与标签 | [`StudentPolicyTrainer.run` / `_produce_actions` / `_get_ground_truth_actions`](https://github.com/NVlabs/HOVER/blob/8088f6cfb42a8f307dc614735197796a86ce8490/neural_wbc/student_policy/neural_wbc/student_policy/student_policy_trainer.py) | 学生进入 rollout 后仍查询教师动作并把标签写入存储 |
| 学生动作损失 | [`StudentPolicyTrainer._update_student_network`](https://github.com/NVlabs/HOVER/blob/8088f6cfb42a8f307dc614735197796a86ce8490/neural_wbc/student_policy/neural_wbc/student_policy/student_policy_trainer.py) | 对学生动作与教师标签求批量范数并更新网络；归约形式与论文平方 L2 写法不完全相同，复现须按锁定版本审计 |

官方仓库为 Apache-2.0；本手册不复制其实现、配置、权重或运动数据。

## 适用边界与局限

作者只明确把“自动模式切换模块”列为未来工作。独立工程判断还包括：实机定量集只有 20 条站立动作；行走中突变切换与 Vision Pro 随机遮挡主要是定性演示；训练掩码在一个回合内固定，而展示包含运行时突变，两者分布并不完全等价；通用策略与专用策略共享同一数据、重定向、奖励和训练家族，比较不覆盖其他算法栈。

HOVER 解决的是控制接口复用，不是碰撞避免、人体接触安全、故障检测或形式化稳定性。模式掩码也不能代替限位、急停和安全监督器。

## 可执行但有边界的结论

当一个机器人项目确实需要根命令、关节命令和稀疏关键点三类上层接口时，可以先定义原子命令字典和显式掩码，再比较“完整运动教师蒸馏”与“相同掩码的端到端 RL”。验收必须分开测固定模式、未见稀疏组合、运行时切换和实机动态动作；不能只凭单一通用策略的平均分替代每种模式的失效测试。

本文不提供可直接下发的增益、力矩、速度或限位参数。实机使用应遵守仿真优先、命令限幅、急停、隔离区和机器人专用审查。
