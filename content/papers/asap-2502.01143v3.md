# ASAP：用残差动作把训练仿真对齐到实机动力学

来源：[arXiv:2502.01143v3](https://arxiv.org/abs/2502.01143v3) · [固定提交官方代码](https://github.com/LeCAR-Lab/ASAP/tree/df5320cc47dd8cad97961bdfabfe402dd62ad999)

解读范围：完整 18 页正文、消融与附录。

## 方法主线

ASAP 先在 Isaac Gym 用重定向人体动作训练 23-DoF G1 跟踪策略，再把预训练策略部署到实机采集状态—动作轨迹。把实机动作在仿真中重放会产生下一状态误差；一个残差策略接收当前仿真状态与记录动作，输出 `Δa`，令仿真用 `a_real + Δa` 演化，并以接近实机下一状态且修正尽量小为奖励。最后冻结残差模型，把它当作“对齐后的训练动力学”来微调主策略；实机部署只运行微调后的主策略，不携带残差模型。

这与学习 delta dynamics 不同：残差直接作用在控制输入，更容易保持模拟器自身积分与接触结构。实机因数据/损坏限制只学四个踝关节残差，而非仿真研究中的全 23 关节模型。

## 关键证据

- Figure 2：预训练/实机采集 → delta action 学习 → 对齐仿真微调 → 无 delta 模型实机部署。
- Figure 4/5：Vanilla、SysID、Delta Dynamics 与 Delta Action 的结构差异；长时开放回放中 delta action 累积误差最小。
- Table III：1.0 s 开放回放时，ASAP 在 IsaacSim/Genesis 的全局误差 37.9/36.9 mm，显著低于 OpenLoop 80.8/82.5。
- Table IV：困难动作跨 IsaacSim/Genesis 均保持 100% 成功；Genesis 全局误差 129 mm，对照 Vanilla 175、SysID 186、DeltaDynamics 190。
- Table V：实机 kick 与 OOD “Silencer”全局误差从 61.2→50.2、159→112 mm；这里只是两个代表动作。
- Figure 10–12：数据量、训练 horizon、修正范数和使用方式均有消融；最佳均匀动作噪声仍弱于学得的非均匀残差。

最强证据是跨两个未训练模拟器的 Table III/IV，因为它既比较动力学对齐，又比较用对齐模型微调后的闭环结果。实机 Table V 很关键但样本动作少，不能单独支持“通用实机动力学模型”。

## 论文—代码映射

| 组件 | 固定提交符号 | 对应关系 |
|---|---|---|
| 开环残差动作 | `DeltaA_OpenLoop._compute_torques` / `get_open_loop_action_at_current_timestep` | 把学习修正与记录动作相加后进入 PD 力矩计算 |
| 小修正正则 | `DeltaA_OpenLoop._reward_minimal_action_norm` 等 | 对应论文 delta action 范数权衡及其消融 |
| 用 delta 模型微调 | `DeltaA_ClosedLoop._compute_torques` / `get_closed_loop_action_at_current_timestep` | 主策略动作与冻结残差动作在训练动力学中组合，并支持缩放/无残差共训 |
| 双策略 rollout | `PPODeltaA._rollout_step` | 同时产生残差动作与载入主策略动作，送入 delta 环境 |

官方提交为 MIT；README 的旧 TODO 与实际文件树不同，因此以提交内容为准。

## 局限与安全边界

作者明确报告：激烈动作导致电机过热和硬件故障，两台 G1 在采集期间受到不同程度损坏；需要 MoCap；完整 23-DoF 实机残差估计约需 400+ 片段，实际只能用 100 片段训练 4-DoF 踝模型。残差能复现实机失败不代表辨识出真实物理，也可能吸收传感/控制栈偏差；换固件、负载、温度或磨损后必须重新验证。

工程上应把 delta action 当作有版本和 ODD 的校准层，先验证它能重现失败，再比较微调、固定点或噪声基线。本文绝不把论文跳跃距离或动作参数当作复现实机许可；高动态训练必须有热/电流监测、跌倒防护、独立急停、低能量逐级测试和硬件损伤预算。
