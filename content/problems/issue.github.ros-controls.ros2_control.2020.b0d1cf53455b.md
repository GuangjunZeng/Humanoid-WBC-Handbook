<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.ros-controls.ros2_control.2020.b0d1cf53455b.md>)

# controller_manager 按旧文档配置 `memlock 102400` 仍锁定失败时，应怎样处理？

> **当前答案：** 该线程中，原作者把 memlock 改为 `unlimited` 后警告消失；第二位用户证明简单扩大到 `1024000` 只让节点先启动，随后仍可能 `std::bad_alloc`，改成 unlimited。项目成员确认 unlimited 可用，并把 `lock_memory=false` 作为明确放弃内存锁定时的备选。工程上应核对进程峰值内存和 page fault，而不是复制旧的 102400 或任意十倍值。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 内存锁定（memory locking）
- 锁定上限（memlock limit）
- 缺页异常（page fault）
- 内存分配失败（bad allocation）

## 问题与症状

- 为什么按旧文档设置 `memlock 102400` 仍无法锁定 controller_manager 内存？
- 102400 限额时报 `Unable to lock the memory`；1024000 可启动但复现者随后遇到 `std::bad_alloc`。

## 环境

- 原帖：Ubuntu、ROS 2 Humble、RPi 4、自定义 RT kernel；复现者：Ubuntu 24.04 Jazzy + PREEMPT_RT、ur_robot_driver。

## 优先排查

- 比较当前 shell/process 的 memlock limit、controller_manager `lock_memory` 参数和进程实际虚拟内存需求；不要只看启动瞬间。

## 可能原因

- 固定 102400 字节不足以覆盖 controller_manager 需要锁定的内存；十倍固定值对另一环境仍不足。

## 处理过程

- 原作者把 soft/hard memlock 改为 unlimited；第二位用户先试 1024000，出现 bad_alloc 后改为 unlimited。

## 有效处理

- 两位参与者采用 unlimited；项目成员确认该设置可用。若明确接受不锁内存，可把 `lock_memory` 设为 false。

## 结果

- 原作者 unlimited 后警告消失；第二位用户确认固定十倍值不足，项目成员确认其环境 unlimited 正常。

## 限制

- `lock_memory=false` 会放弃该内存锁定保证，不是等价实时修复。
- 线程没有给不同机器人、DDS 和控制器组合所需的最小可审计字节数；unlimited 仍受系统和部署策略约束。

## 安全提示

- 真机前应在峰值控制器/消息负载下检查 page fault、RSS、OOM 和周期抖动；不要用任意固定倍数替代容量验证。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于 Humble/Jazzy controller_manager 内存锁定；部署系统的 limits/PAM/container 配置需分别核对。

## 原始来源

- **来源定位：** ros2_control #2020 原帖 A/B；第二用户 bad_alloc 2660327112；项目确认 2660331588
- **问题或回复：** [controller_manager 内存锁定不能沿用过小的固定 memlock 上限](<https://github.com/ros-controls/ros2_control/issues/2020#issuecomment-2660331588>)
- **根来源：** [controller_manager 内存锁定不能沿用过小的固定 memlock 上限](<https://github.com/ros-controls/ros2_control/issues/2020>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
