<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.ros-controls.ros2_control.3145.ce7442003c46.md>)

# publish_activity 的 RT 路径共享 recursive_mutex 时，最终合并的缓解方案是什么？

> **当前答案：** 维护者没有采用 atomic flag 搬移发布，而是要求使用 realtime_tools 已有的 priority-inheritance recursive mutex。PR #3197 将 controllers_lock_ 及全部调用点迁移到 realtime_tools::prio_inherit_recursive_mutex，Windows 保留 std::recursive_mutex 回退；PR 经 review 和测试后合并。它解决的是源码层面的优先级反转风险，不应写成已经量化了 jitter 改善。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 可信度很高
- **评级理由：** 问题已闭环、环境明确且无冲突，并有正式资料或独立复现的精确引用。

## 关键术语

- 优先级反转（priority inversion）
- 优先级继承互斥锁（priority-inheritance mutex）
- 实时线程（real-time thread）
- 递归互斥锁（recursive mutex）

## 问题与症状

- controller_manager 的 RT update 路径与非 RT 服务共享 recursive_mutex 时，应怎样降低优先级反转风险？
- 这是源码审计发现的潜在 priority inversion/jitter 风险，线程没有给出机器人上的实测卡顿或 benchmark。

## 环境

- ros2_control master commit a03be840 发现问题；PR 在 ROS 2 Rolling 构建验证；POSIX 使用 realtime_tools PI mutex，Windows 回退 std::recursive_mutex。

## 优先排查

- 从 SCHED_FIFO update() 追踪 fallback activation 到 publish_activity()，再检查 controllers_lock_ 是否也被 executor/service 路径持有。

## 可能原因

- 标准 recursive_mutex 不提供此处需要的优先级继承，低优先级线程持锁时可能阻塞 RT 线程。

## 处理过程

- 最初 PoC 用 atomic flag 进行 RT→non-RT 触发；维护者选择更小范围的 PI recursive mutex 替换。

## 有效处理

- PR #3197 将 controllers_lock_ 迁移为 realtime_tools::prio_inherit_recursive_mutex，并保留 Windows 回退。

## 结果

- PR 获两位维护者批准、修改行测试覆盖后合并。

## 限制

- 合并证明代码方案被接受，不证明原路径曾产生可测 jitter，也不提供性能改善数值。
- Windows 回退路径没有 PI mutex，适用性不同。

## 安全提示

- 锁替换后仍应在 fallback、服务并发和控制器切换压力场景测量最坏延迟。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- ros2_control controller_manager 使用共享 controllers_lock_ 的 POSIX 平台；Windows 为标准 mutex 回退。

## 原始来源

- **来源定位：** ros2_control #3145 评论 4160585534、4191555039、4200304531；PR #3197；merge commit 308e4aab78958b730d847c0054c69f2cefabd8db
- **问题或回复：** [ros2_control 实时路径共享递归锁改用优先级继承 mutex](<https://github.com/ros-controls/ros2_control/issues/3145#issuecomment-4200304531>)
- **根来源：** [ros2_control 实时路径共享递归锁改用优先级继承 mutex](<https://github.com/ros-controls/ros2_control/issues/3145>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
