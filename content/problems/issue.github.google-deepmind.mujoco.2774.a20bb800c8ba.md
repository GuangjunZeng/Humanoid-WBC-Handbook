<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.google-deepmind.mujoco.2774.a20bb800c8ba.md>)

# 看到 MJX 与 MuJoCo CPU 在 plane-capsule 接触下的 force 或 `qacc` 不一致时，是否可以把 `b_norm < 0.5` fallback 认定为已确认根因并直接替换？

> **当前答案：** 目前不能。线程中的详细最小例子确实纠正了早期对 `normalize_with_norm` 返回值的理解，并展示了 fallback 可能生成非正交 frame、且差异集中在 pyramidal friction 的证据；但维护者只是重新打开 Issue 并表示将检查，尚未确认根因或合并修复。可把 frozen-state、contact frame、cone type 和 `qacc_warmstart=0` 的对比流程用于复现，不能把作者提议的 `math.orthogonals` 替换写成通用有效修复。

## 可信度与证据状态

- **手册分级：** `L0 未解决或存在冲突`
- **分级说明：** 当前讨论未解决，或不同来源之间仍存在冲突。
- **问题状态：** `conflicting`
- **证据状态：** `issue_candidate`
- **来源内评级：** 需要实际验证
- **评级理由：** 解答尚未闭环或存在冲突；来源结论存在未解决冲突。

## 关键术语

- 接触坐标系（contact frame）
- 正交归一基（orthonormal basis）
- 金字塔摩擦锥（pyramidal friction cone）
- 预热加速度（qacc warm-start）

## 问题与症状

- MJX plane-capsule 的 `b_norm < 0.5` fallback 是否会生成非正交切向基，并放大 CPU-MJX 动力学差异？
- 跟进报告 `b_norm=0.4407495` 时 fallback 触发；给定 `n=[0.3,0.4,0.8660254]` 会选 `b=[0,1,0]`，从而 `n·b=0.4`。
- 同一 common-world point Jacobian 约差 `5.82e-18`，但 active contact tangent 不同；pyramidal friction 下 force 与 `qacc` 差异明显，`condim=1` 和 isotropic elliptic 基本一致。

## 环境

- 详细跟进固定到 MuJoCo commit 6882095；一个 plane、一个 free capsule、一个显式 contact pair；CPU double 与 jitted MJX float64，同一 frozen state、fresh data、`qacc_warmstart=0`。

## 优先排查

- 冻结同一状态、清空 warmstart，分别比较 common-world Jacobian、contact frame、friction cone 类型、constraint force 与 `qacc`；不要只比较接触点位置。

## 可能原因

- 跟进作者指出 fallback 复用了 `math.orthogonals` 的轴选择，但缺少后续投影和重新归一化；维护者尚未正式确认该根因。

## 处理过程

- 作者提出在很小容差下使用 `math.orthogonals(n)[0]`，并报告替换选择后 parity 恢复；尚无维护者审核或合并补丁。

## 有效处理

- 当前没有可登记的正式修复；只能把该替换作为待验证实验，不应直接升级生产或训练基线。

## 结果

- Issue 从关闭状态重新打开；维护者表示会检查，线程截至采集时没有进一步结论。

## 限制

- 现有数据来自一个最小 frozen-state 例子；尚未证明对所有 plane-capsule 姿态、cone 类型和 MuJoCo 版本都成立。

## 安全提示

- 若 WBC 依赖摩擦方向或接触力阈值，应在目标版本对 CPU/MJX 接触 frame 和 cone 配置做回归测试。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 仅直接适用于 commit 6882095 和原帖最小 plane-capsule frozen-state 配置；其他版本与接触几何需复现。

## 原始来源

- **来源定位：** MuJoCo #2774 详细纠正与复现实验 5082490175；维护者重新打开 5104526098
- **问题或回复：** [MJX plane-capsule 切向基 fallback 的数值差异仍待维护者确认](<https://github.com/google-deepmind/mujoco/issues/2774#issuecomment-5082490175>)
- **根来源：** [MJX plane-capsule 切向基 fallback 的数值差异仍待维护者确认](<https://github.com/google-deepmind/mujoco/issues/2774>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
