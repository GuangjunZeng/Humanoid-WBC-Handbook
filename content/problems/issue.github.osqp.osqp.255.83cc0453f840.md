<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.osqp.osqp.255.83cc0453f840.md>)

# OSQP 旧版无法识别 `x≤0, x≥1e-4` 这类不可行问题时，项目合入了什么修复？

> **当前答案：** Issue #255 与合并 PR #256 把旧版 certificate 判据中的负 epsilon 改为正 epsilon，并用独立的 `OSQP_DIVISION_TOL` 判断 certificate 向量非零；文档也对齐最终论文。该变更确实作为 `develop-0.x` 的正式修复合入。由于后来的 #485 报告正 epsilon 在 singleton feasible region 上可能产生反向误判，这张经验只证明历史合并与原 MRE，不证明判据覆盖所有退化边界。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 可信度很高
- **评级理由：** 问题已闭环、环境明确且无冲突，并有正式资料或独立复现的精确引用。

## 关键术语

- 不可行证书（infeasibility certificate）
- 原始不可行容差（primal infeasibility tolerance）
- 严格不等式（strict inequality）
- 退化可行域（degenerate feasible region）

## 问题与症状

- 旧 OSQP 为什么可能永远检测不到一个简单不可行 QP，项目当时合入了什么修复？
- 对任意 `eps_prim_inf` 都能构造一个间隔等于该 epsilon 的不可行标量问题，使旧条件无法给出 certificate。

## 环境

- 2020 年 `develop-0.x` 分支；Issue 给出 CVXPY 标量 MRE，PR 给出直接 OSQP Python MRE。

## 优先排查

- 对照代码、最终论文 Section 3.4 与当前 certificate inequality；保留退化/单点可行域回归集。

## 可能原因

- 维护者确认旧代码把 negativity test 写成负 epsilon，且用 `eps_*_inf` 自身判断 certificate 向量非零。

## 处理过程

- PR #256 把 primal/dual 条件换成正 epsilon，并用 `OSQP_DIVISION_TOL` 处理零范数；同步文档。

## 有效处理

- 历史上升级到包含 merge commit `fad7cf2` 的 `develop-0.x` 修复旧版 non-detection；不能据此断言所有退化边界都正确。

## 结果

- PR 已批准、合并；作者/维护者给出修复前失败的最小例。

## 限制

- PR 没有在可见 diff 中加入专门测试文件；#485 后来报告正 epsilon 对 singleton feasible region 的相反问题，本问题综合等级必须保留冲突。

## 安全提示

- 升级 solver 后同时测试明确 infeasible、普通 feasible 和 singleton/退化 feasible 三类 QP。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 直接对应 2020 年 `develop-0.x` 修复；后续版本和 singleton feasible case 必须与 #485 一并验证。

## 原始来源

- **来源定位：** OSQP #255 维护者确认 639451057/639472858；合并 PR #256
- **问题或回复：** [OSQP 旧版不可行证书符号修复已合并但后来出现边界争议](<https://github.com/osqp/osqp/issues/255#issuecomment-639472858>)
- **根来源：** [OSQP 旧版不可行证书符号修复已合并但后来出现边界争议](<https://github.com/osqp/osqp/issues/255>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
