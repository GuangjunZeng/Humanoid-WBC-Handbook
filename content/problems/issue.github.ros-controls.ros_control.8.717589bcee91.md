<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.ros-controls.ros_control.8.717589bcee91.md>)

# 为什么旧 ros_control 的 `unlockAndPublish()` 会让实时循环触发 futex 系统调用？

> **当前答案：** 维护者给出的直接原因是：实时线程持有发布锁时，非实时线程已经阻塞等待同一把锁；在这种状态下，实时线程随后执行 unlock 就不再是实时安全操作。原作者通过注释 `unlockAndPublish()` 和 RTAI syscall 号完成 A/B 定位。项目称默认分支已修复，但没有提供 commit 或发布版本，所以只能把该结论用于识别这种锁竞争模式。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 实时发布器（realtime publisher）
- 互斥锁（mutex）
- 快速用户态互斥锁（futex）
- 优先级反转（priority inversion）

## 问题与症状

- 为什么 `RealtimePublisher::unlockAndPublish()` 会在实时线程触发 futex 系统调用？
- 调用 `unlockAndPublish()` 时出现 `LXRT CHANGED MODE (SYSCALL)`，syscall 号为 240；注释该调用后不再出现。

## 环境

- 2012 年 ros_control；32 位 RTAI 用户态实时循环；具体提交未给出。

## 优先排查

- 重启或重新插入 RTAI 模块后查看 dmesg 中首个 hard-to-soft syscall；原作者确认机器为 32 位，因此 240 对应 futex。

## 可能原因

- 维护者确认：实时线程持锁期间，非实时线程开始阻塞等待；此后实时线程 unlock 会进入非实时安全路径。

## 处理过程

- 对 `unlockAndPublish()` 做 A/B 注释，并结合 RTAI/dmesg 的 syscall 编号确认。

## 有效处理

- 项目维护者称已在当时默认分支修复该锁竞争路径。

## 结果

- 根因被维护者明确说明；线程未给修复后 syscall 计数、commit 或 release。

## 限制

- 结论绑定旧 ros_control/RTAI 实现；不能从该线程推断现代 `realtime_tools` 的内部实现。

## 安全提示

- 对 WBC 实时发布路径应在目标内核上做锁竞争压力测试，不能只在无订阅者或低负载条件下验收。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 直接适用于该线程的旧 RealtimePublisher/RTAI 实现；当前版本需重新测量。

## 原始来源

- **来源定位：** ros_control #8 syscall 观测 10822469；维护者根因与修复说明 13088686
- **问题或回复：** [RealtimePublisher 解锁时的非实时等待者会破坏实时安全](<https://github.com/ros-controls/ros_control/issues/8#issuecomment-13088686>)
- **根来源：** [RealtimePublisher 解锁时的非实时等待者会破坏实时安全](<https://github.com/ros-controls/ros_control/issues/8>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
