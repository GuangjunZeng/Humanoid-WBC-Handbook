<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.osqp.osqp.109.0f55861eec4d.md>)

# OSQP 报不可行而 quadprog 给出解时，为什么要先检查是否用 `-1000` 代替了 `-inf`？

> **当前答案：** 因为这会改变可行域。该线程中维护者验证 OSQP 的不可行证书成立，并发现 quadprog 的 `x` 对 OSQP 输入有 `min(Ax-l)=-219.2470` 的违反。原作者随后确认 inequality lower bound 被写成 `-1000`，而不是 `-inf`/`-OSQP_INFTY`；改成真正无下界后问题解决。比较 solver 前必须先证明两边实际接收的是同一个 `l≤Ax≤u`。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 不可行证书（infeasibility certificate）
- 可行域（feasible region）
- 有限替代边界（finite surrogate bound）
- 无穷边界（infinite bound）

## 问题与症状

- OSQP 报 primal infeasible、另一个 solver 却给出解时，如何检查是否真的在解同一个约束问题？
- OSQP 50 iterations 报 primal infeasible，quadprog 报 constraints satisfied。

## 环境

- OSQP 0.4.1 MATLAB interface；64 variables、82 constraints；对照 MATLAB quadprog。

## 优先排查

- 验证 OSQP 的 `prim_inf_cert` 条件，并把对照 solver 的 `x` 代回同一 `l≤Ax≤u` 逐项检查。

## 可能原因

- 作者最终确认把不存在的下界编码为 `-1000`，而非真正的 `-inf`，两 solver 实际没有被同样约束。

## 处理过程

- 维护者计算不可行证书并验证 quadprog 解违反 OSQP 下界；作者随后检查建模代码。

## 有效处理

- 对无下界的 inequality 使用接口支持的 `-inf` 或 C 端 `-OSQP_INFTY`，不要用任意“大负数”代替。

## 结果

- 原作者明确说明改用 `-inf` 后问题解决。

## 限制

- 该结论只对应这次建模差异；若两个接口逐元素输入一致，仍需检查缩放、容差和 solver bug。

## 安全提示

- 发布前保存规范化 QP 数据并让两个接口逐元素校验，避免单位变化后有限 surrogate bound 进入真实可达区间。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于用有限 surrogate 编码无边界的 OSQP/跨 solver 对照；数值范围不同也应重复检查。

## 原始来源

- **来源定位：** OSQP #109 证书核对 442394129；作者根因与修复 442778706
- **问题或回复：** [用 -1000 代替 -inf 会把 OSQP inequality 改成另一个 QP](<https://github.com/osqp/osqp/issues/109#issuecomment-442778706>)
- **根来源：** [用 -1000 代替 -inf 会把 OSQP inequality 改成另一个 QP](<https://github.com/osqp/osqp/issues/109>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
