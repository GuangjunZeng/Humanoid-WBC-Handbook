<!-- Generated by wbc-handbook render-problems; do not edit. -->

# TSID 只设置速度界时出现非预期 QP 不可行，应如何检查 TaskJointPosVelAccBounds 的默认位置界？

> **当前答案：** 检查旧版构造函数是否把 qMin/qMax 写成 +1e10/-1e10。该顺序使下界大于上界；常规显式重设位置界会掩盖问题，但只设置速度界时可能触发不可行。已合并 PR #159 将默认值改为 qMin=-1e10、qMax=+1e10。旧版除回移修复外，也必须在使用前显式设置正确位置界。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 可信度很高
- **评级理由：** 问题已闭环、环境明确且无冲突，并有正式资料或独立复现的精确引用。

## 关键术语

- 关节位置界（joint position bounds）
- 速度界（velocity bounds）
- 不可行问题（infeasible problem）
- 二次规划（Quadratic Programming, QP）

## 问题与症状

- 只配置速度限制而沿用 TaskJointPosVelAccBounds 默认位置界时，为什么 QP 可能不可行？
- 默认 qMin=+1e10、qMax=-1e10，导致 qMin>qMax；依赖默认位置界时可能生成不可行约束。

## 环境

- 2022-03 的 TSID TaskJointPosVelAccBounds 旧实现；具体发行版本和机器人未说明。

## 优先排查

- 打印或检查 TaskJointPosVelAccBounds 构造后的 qMin/qMax；确认是否在使用前显式重设位置界。

## 可能原因

- 构造函数中默认上下界的正负号交换。

## 处理过程

- 提问者定位到构造代码；维护者确认并请求提交 PR。

## 有效处理

- 采用已合并 PR #159，把默认 qMin 改为 -1e10、qMax 改为 +1e10；旧版也可显式设置正确位置界避免依赖错误默认值。

## 结果

- PR #159 于 2022-03-02 合并，维护者后续明确回复该问题已由 #159 修复。

## 限制

- 原线程没有给出一个实际 QP 失败日志或新增回归测试；影响条件是未覆盖默认位置界。

## 安全提示

- 实机前仍应检查配置后的实际上下界，不能只依赖极大默认值作为安全限位。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于包含错误默认值的 TSID 旧版本；若调用方总是显式覆盖位置界，该特定错误不会直接触发。

## 原始来源

- **来源定位：** Issue #158，维护者确认 issuecomment-1056957094 与修复确认 issuecomment-1292023533；已合并 PR #159
- **问题或回复：** [TSID 旧版位置上下界默认值写反会让仅设置速度界的任务潜在不可行](<https://github.com/stack-of-tasks/tsid/issues/158#issuecomment-1292023533>)
- **根来源：** [TSID 旧版位置上下界默认值写反会让仅设置速度界的任务潜在不可行](<https://github.com/stack-of-tasks/tsid/issues/158>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
