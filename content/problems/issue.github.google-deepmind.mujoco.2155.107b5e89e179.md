<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.google-deepmind.mujoco.2155.107b5e89e179.md>)

# 需要在高度场地形上做 ray casting 时，是否应等待 MJX 支持 `mjx.ray` 的 hfield geom？

> **当前答案：** 按 MuJoCo 维护者在该线程的最终决定，不应把当前方案建立在 MJX 即将补齐此功能的假设上：该能力不计划加入 MJX。维护者明确指向 MuJoCo Warp 的 `ray`，其官方 API 文档包含 height fields。工程上应把它作为候选后端重新评估，但原帖没有证明两条后端在性能、数值或接口上完全等价。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 可信度很高
- **评级理由：** 问题已闭环、环境明确且无冲突，并有正式资料或独立复现的精确引用。

## 关键术语

- 高度场（height field, hfield）
- 射线检测（ray casting）
- 测距传感器（rangefinder）
- 计算后端（compute backend）

## 问题与症状

- MJX 的 `mjx.ray` 能否用于 hfield 地形的射线检测？
- `mjx.ray` 当时没有 hfield geom 实现，无法直接为 hfield locomotion terrain 生成所需射线结果。

## 环境

- 原始请求针对 MJX 的 `mjx.ray` 与 hfield geom；线程未固定 MuJoCo 版本。

## 优先排查

- 先确认目标后端是 MJX 还是 MuJoCo Warp，再核对所需 geom 类型是否在该后端 `ray` API 中受支持。

## 可能原因

- 这是维护者确认的功能边界，不是原帖已经定位出的运行时故障。

## 处理过程

- 原帖提出把 CPU `mj_rayHfield` 移植到 MJX；后续贡献者询问是否可以实现。

## 有效处理

- 若需求是 hfield ray casting，按维护者指向评估 MuJoCo Warp `ray`；线程没有声称 MJX 与 MuJoCo Warp 可无成本互换。

## 结果

- 维护者明确 MJX 不计划此功能，并把 Issue 关闭；官方 MuJoCo Warp `ray` 文档列出 height fields。

## 限制

- 该结论只回答项目公开计划与后端能力选择，不比较两个后端的性能、数值一致性或完整迁移成本。

## 安全提示

- 把地形感知输入用于真机 WBC 前，还需独立验证量程、遮挡、坐标系和时延。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于需要 hfield ray casting、正在 MJX 与 MuJoCo Warp 间选型的项目；具体版本与迁移代价需另行验证。

## 原始来源

- **来源定位：** MuJoCo #2155 维护者最终回复 4707886038
- **问题或回复：** [MJX 不计划补齐 hfield ray，项目指向 MuJoCo Warp](<https://github.com/google-deepmind/mujoco/issues/2155#issuecomment-4707886038>)
- **根来源：** [MJX 不计划补齐 hfield ray，项目指向 MuJoCo Warp](<https://github.com/google-deepmind/mujoco/issues/2155>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
