<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.isaac-sim.isaaclab.3823.5ea342a5c339.md>)

# Isaac Lab 的显式执行器在 decimation 内会保持同一力矩，还是每个 simulation step 重算？

> **当前答案：** 维护者明确说明：显式执行器（explicit actuator）的 effort 随每次 simulation step 调用 `write_data_to_sim()` 而重新计算；通常保持到下个控制周期的是 actuator target，而不是必然保持同一 effort。隐式执行器（implicit actuator）的 PhysX 内部 PD 也会在每个 sim step 用当前关节状态重算；显式路径会把 PhysX 内部 PD gains 设为零，使显式计算的 effort 作为 pass-through 写入模拟器。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 显式执行器（explicit actuator）
- 隐式执行器（implicit actuator）
- 控制降采样（decimation）
- 比例微分控制（Proportional-Derivative control, PD）

## 问题与症状

- 显式 actuator 是否只在每个 policy step 计算一次力矩并跨 decimation 保持？
- 隐式 PhysX PD 是否每个 sim step 重算力矩？
- 用户原先把显式路径画成同一力矩在 5 个 sim steps 内保持。

## 环境

- 原帖给出示例：policy 100 Hz、simulation 500 Hz、decimation 5；没有固定 Isaac Lab commit。

## 优先排查

- 沿 `Articulation.write_data_to_sim()`、`_apply_actuator_model()` 和内部 PhysX PD gains 的写入路径检查调用频率。

## 可能原因

- 问题来自把 target update frequency 与 actuator effort compute frequency 混为一谈。

## 处理过程

- 维护者直接按源码调用路径逐项回答显式与隐式两类 actuator。

## 有效处理

- 建模时把 target 通常按 control frequency 更新、explicit compute 按每次 sim-step write 调用执行、implicit PhysX PD 按每 sim step 重算三件事分开。

## 结果

- 维护者确认用户图中的显式力矩恒定假设不正确；隐式 PD 每 sim step 重算的理解正确。

## 限制

- 回复使用当时 `main` 的源码链接，未固定 commit；自定义环境若不是每个 sim step 调 `write_data_to_sim`，必须按实际调用链核对。

## 安全提示

- 真机驱动器内部电流环、速度环和 Isaac Lab actuator 不是同一层级，不能直接用此频率关系替代硬件环路说明。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于该回复所对应的 Isaac Lab 调用链；需以目标版本和自定义 step loop 的真实 `write_data_to_sim` 调用频率复核。

## 原始来源

- **来源定位：** IsaacLab #3823 维护者回复 3446108549
- **问题或回复：** [Isaac Lab 显式与隐式执行器在 decimation 内的更新语义](<https://github.com/isaac-sim/IsaacLab/issues/3823#issuecomment-3446108549>)
- **根来源：** [Isaac Lab 显式与隐式执行器在 decimation 内的更新语义](<https://github.com/isaac-sim/IsaacLab/issues/3823>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
