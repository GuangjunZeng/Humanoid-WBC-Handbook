<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.isaac-sim.isaaclab.5918.5efe80a96f8d.md>)

# Isaac Lab 的 nested URDF 只有根 link 获得 `disable_gravity` 和 mass properties，应如何修复和验收？

> **当前答案：** 根因是 `apply_nested` 在根 link 首次成功后停止下钻，跳过了嵌套子 links。合并 PR #6377 给 decorator 增加 `stop_on_success`，并让 rigid-body 与 mass property writers 使用 `stop_on_success=False`；默认仍为 true，避免无关 schema 行为变化。验收不能只看 cfg，应遍历全部 rigid bodies，逐项确认 `disableGravity`、`physics:mass` 等目标属性覆盖 N/N。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 可信度很高
- **评级理由：** 问题已闭环、环境明确且无冲突，并有正式资料或独立复现的精确引用。

## 关键术语

- 嵌套刚体层级（nested rigid-body hierarchy）
- 生成属性（spawn properties）
- 遍历早停（traversal early stop）
- 回归测试（regression test）

## 问题与症状

- 为什么 nested URDF 的 rigid-body/mass spawn properties 只作用于根 link？
- 29 links 中只有 1 个得到 `disable_gravity=True`，手端约下垂 0.2 m；contact report API 也是 1/N，子 link ContactSensor 报找不到 reporter API。

## 环境

- IsaacLab a4a7602f29 v3.0.0-beta 与 8ef1bf7a8b beta2、Isaac Sim 6.0.0-rc.22、Ubuntu 24.04、RTX 5090、CUDA 13.0、driver 580.159.03。

## 优先排查

- 遍历 stage，分别统计 RigidBodyAPI、disableGravity 和 PhysxContactReportAPI 的 body 覆盖数。

## 可能原因

- `apply_nested` 默认认为 physics schema 不会嵌套，wrapped function 在根 link 成功后停止遍历 children。

## 处理过程

- PR #6377 给 `apply_nested` 增加默认保持兼容的 `stop_on_success`，rigid-body/mass writers 显式设置为 false。

## 有效处理

- 升级到包含 PR #6377、merge commit 10144da 的版本，并用 stage traversal 验证每个 nested body 的属性。

## 结果

- PR 已合并，三层嵌套刚体 regression test 同时断言 `disableGravity` 和 `physics:mass` 在 3/3 links 生效。

## 限制

- #6377 只对 rigid-body 与 mass writers 放宽早停；joint、articulation root 和 collision writers 仍保留默认行为。
- 对曾按旧的部分生效行为调过 baseline 的 nested asset，升级后任务行为可能变化。

## 安全提示

- 不要只看 cfg；spawn 后应逐 body 审计最终 USD/PhysX 属性，尤其是 gravity、mass、contact report 和 depenetration limits。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于 Isaac Sim 6.0 importer 或自定义 USD 的 body-under-body nested hierarchy。

## 原始来源

- **来源定位：** IsaacLab #5918 原帖 1/N 复现、关闭说明 5174104792；合并 PR #6377
- **问题或回复：** [Isaac Lab 嵌套 URDF 的 spawn 属性只落到根 link](<https://github.com/isaac-sim/IsaacLab/issues/5918#issuecomment-5174104792>)
- **根来源：** [Isaac Lab 嵌套 URDF 的 spawn 属性只落到根 link](<https://github.com/isaac-sim/IsaacLab/issues/5918>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
