<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.ros-controls.ros_control.130.70b2a7939792.md>)

# 怎样把 ros_control 的硬件读写和 ControllerManager 放进 Orocos RTT 实时循环？

> **当前答案：** 原线程的最小结构是在 RTT `configureHook()` 中创建 hardware interface 与 `ControllerManager`，在周期 `updateHook()` 中依次执行 `robot->read()`、`controllerMgr->update(time, period)`、`robot->write()`。该模式后来被整理为 `rtt_ros_control_example`，并被另一位参与者用于实现 `hal_ros_control`。它说明的是线程组织方式，不提供现代 ROS 2 或特定硬件的实时保证。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 读算写循环（read-compute-write loop）
- 实时执行器（realtime executor）
- 周期钩子（update hook）
- 硬件接口（hardware interface）

## 问题与症状

- 怎样用外部实时调度器运行 ros_control 的 read-update-write 循环？
- 怎样处理 ROS callbacks 和运行时参数而不让非实时线程阻塞实时循环？
- 直接把 ROS callbacks、services 和控制周期混在同一执行模型中，会留下调度优先级、互斥锁和系统调用边界不明确的问题。

## 环境

- ROS 1 ros_control；PREEMPT_RT/Xenomai 讨论；Orocos RTT 示例；2013–2018 年线程。

## 优先排查

- 明确标注 configure/read/update/write/callback 各自所在线程，并检查共享数据是否经过实时安全交换结构。

## 可能原因

- 非实时 callback 与实时循环共享普通锁，或在实时线程触发设置变更和系统调用，会产生优先级反转与不可预测时延。

## 处理过程

- 在 RTT `configureHook()` 构造 hardware interface 与 ControllerManager；在 `updateHook()` 执行 `read → update → write`。
- 为每个组件建立独立 `CallbackQueue`，由非实时线程 spin，并用 `RealtimeBuffer` 交接数据。

## 有效处理

- 把周期调度交给 RTT/HAL 等实时执行器，同时把 ROS API 回调隔离在非实时线程。

## 结果

- 线程参与者发布最小 `rtt_ros_control_example`；另一位用户明确称这些讨论帮助其开发 `hal_ros_control`。

## 限制

- 示例属于旧 ROS 1/RTT；`ros::Time::now()` 在同仓库另有旧实时安全问题，不能直接照抄时间源。
- 线程没有给统一的 WCET、jitter 或不同内核对照。

## 安全提示

- 真机 WBC 运行时参数必须经过边界检查和原子/实时安全交接；非实时 callback 不应持有实时线程所需的普通互斥锁。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于旧 ROS 1 ros_control + RTT/HAL 外部实时调度器架构。

## 原始来源

- **来源定位：** ros_control #130 最小循环 52055471；后续例子 54360309；HAL 使用反馈 407977464
- **问题或回复：** [把 ros_control 的周期循环与 ROS 回调线程明确分离](<https://github.com/ros-controls/ros_control/issues/130#issuecomment-52055471>)
- **根来源：** [把 ros_control 的周期循环与 ROS 回调线程明确分离](<https://github.com/ros-controls/ros_control/issues/130>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
