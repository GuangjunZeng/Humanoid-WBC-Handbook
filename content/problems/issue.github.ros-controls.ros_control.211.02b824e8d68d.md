<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.ros-controls.ros_control.211.02b824e8d68d.md>)

# ros_control 中 `prepareSwitch()` 与 `doSwitch()` 应分别在哪个线程执行，为什么？

> **当前答案：** 该线程和已合并 PR #209/#210 将职责分成两层：非实时 callback 中的 `prepareSwitch()` 负责可能耗时的准备与可行性检查；真正提交硬件模式切换的 `doSwitch()` 与 controller start/stop 一起在实时 `update()` 路径发生，并必须非阻塞。这样避免硬件模式在 controller 尚未切换时提前改变。对需多个周期确认的 CANopen 等硬件，线程没有给通用策略，驱动仍必须明确切换期间旧/新命令的处理。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 可信度很高
- **评级理由：** 问题已闭环、环境明确且无冲突，并有正式资料或独立复现的精确引用。

## 关键术语

- 模式切换（mode switching）
- 非实时准备（non-realtime preparation）
- 原子提交（atomic commit）
- 实时安全（realtime safe）

## 问题与症状

- 硬件控制模式为什么不能在非实时 switch service callback 中先于控制器完成切换？
- 慢速硬件怎样与单周期 controller switch 语义共存？
- 旧实现可能先切硬件模式、后启动新 controller，导致命令落入错误模式或出现未定义行为。

## 环境

- ROS 1 ros_control Indigo/Jade；作者在硬件复现；ros_canopen 参与者报告典型切换需 3–4 个周期，部分硬件 5 个以上。

## 优先排查

- 审计 `prepareSwitch/doSwitch/controller start-stop` 的调用线程和实际顺序，并记录硬件模式确认所需周期。

## 可能原因

- `doSwitch()` 错放在只负责调度切换的非实时 ROS callback，而 controller start/stop 实际在实时 update 线程发生。

## 处理过程

- PR #209/#210 保存 start/stop list，把 `doSwitch()` 移到实时 `update()`，并用非实时 `prepareSwitch()` 做可阻塞准备。

## 有效处理

- 采用 `prepareSwitch()` 非实时准备 + `doSwitch()` 实时、非阻塞提交的两阶段接口。

## 结果

- PR #209 和 #210 均已合并；#209 修订后测试通过；ros_canopen 参与者确认在 Indigo 变更上按预期工作。

## 限制

- 这是 ROS 1 Indigo/Jade 历史接口；慢速硬件仍需自行定义切换期间的命令保持、丢弃和同步策略。
- 线程对是否允许多周期切换有过争论，最终合并方案不等同于任意硬件都能单周期完成物理模式切换。

## 安全提示

- 真机模式切换应先在吊架/空载和低增益条件验证，监控旧/新命令、硬件 mode acknowledgement 与急停状态；不得在实时 `doSwitch()` 中阻塞等待总线。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 直接适用于 ROS 1 Indigo/Jade 历史接口；现代 ros2_control 需按当前 lifecycle/switch API 重新核对。

## 原始来源

- **来源定位：** ros_control #211 原帖真机复现；PR #209 merge 57c94c0；PR #210 merge 1ae7a0b；ros_canopen 复测 157007428
- **问题或回复：** [控制器模式切换的 prepareSwitch 与 doSwitch 必须分属非实时和实时阶段](<https://github.com/ros-controls/ros_control/issues/211>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
