<!-- Generated by wbc-handbook render-problems; do not edit. -->

[中文版](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/issue.github.ros-controls.ros_control.10.816004bbb658.md>)

# Why did the old ros_control switch implementation move STL vector `clear()` out of the `update()` real-time branch?

> **Current answer:** The original author found the `clear()` function in the `please_switch_` real-time branch and moved it to the non-realtime path. The maintainer confirmed that the `clear()` calls have been moved to the non-realtime path. There is no proof that a `clear()` call must be heap-allocated, and no commit was provided, so the reusability principle is to audit and isolate container lifecycle operations in the real-time path, rather than mechanically flagging all same-name calls as faults.

## Credibility and evidence status

- **Handbook level:** `L2 Resolved in the source, pending independent verification`
- **Level note:** The source reports the issue as resolved, but this is not yet a general conclusion across robots or versions.
- **Answer status:** `resolved`
- **Evidence status:** `issue_candidate`
- **Source-level grade:** Useful reference
- **Grade rationale:** The environment, processing steps, and results have engineering records, but formal cross-verification or independent replication is missing.

## Key terms

- Realtime path (实时路径)
- Controller switching (控制器切换)
- Dynamic allocation (动态分配)
- Container clearing (容器清理)

## Problem and symptoms

- Why shouldn't the `clear()` function in the real-time `update()` branch be executed during controller switching?
- Code review found `please_switch_` branch clearing an STL vector, and the original author questioned real-time safety.

## Environment

- 2012 ros_control `ControlManager.update`; specific branch and release version not provided.

## Priority diagnostics

- Check container cleanup, destruction, and potential memory management operations in the real-time update path.

## Possible causes

- The original thread considered the `clear()` function in the real-time branch as an operation that should not be retained.

## Attempts

- Move the `clear()` function in the switch list to the non-realtime switch preparation phase.

## Effective fixes

- The maintainer confirmed that the default branch has moved `clear()` to the non-realtime path.

## Result

- The maintainer explicitly confirmed the fix is complete; no jitter or allocation count comparison provided.

## Limitations

- No commit, test, or first fix release; cannot treat all `clear()` calls as necessarily heap-allocated.

## Safety notes

- Static review and runtime allocation/system call detection should be done on real-time WBC paths to avoid relying solely on API names.

## Image and visual evidence

- The source provides no verifiable image, or the current capture contains no visual evidence available for analysis.

## Applicability boundary

- Applies to old ros_control switch implementation and similar real-time container lifecycle audits.

## Original source

- **Source locator:** ros_control #10 original code review; maintainer reply 13155600
- **Problem or reply:** [Controller switching list's `clear()` operation should be moved out of the real-time branch](<https://github.com/ros-controls/ros_control/issues/10#issuecomment-13155600>)
- **Root source:** [Controller switching list's `clear()` operation should be moved out of the real-time branch](<https://github.com/ros-controls/ros_control/issues/10>)

> Community posts and Issues can shorten diagnosis, but they must not be promoted to cross-platform engineering conclusions without independent verification against papers, source code, official documentation, or reproduction.
