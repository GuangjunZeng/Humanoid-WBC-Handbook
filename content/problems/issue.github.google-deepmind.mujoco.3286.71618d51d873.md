<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.google-deepmind.mujoco.3286.71618d51d873.md>)

# MuJoCo sysid 对 LuGre 参数零迭代、零梯度且参数不变时，帖子中的实际原因和修法是什么？

> **当前答案：** 该例不是 LuGre 参数通路失效，而是 Python factory 的 return modifier 缩进在 inner function 内，outer factory 隐式返回 None。sysid.Parameter 收到 None 后不调用 modifier，candidate 值没有进入 spec，残差不变，有限差分梯度自然为零。修法是让 outer factory 返回 modifier；inner modifier 只修改 spec。维护者还单独检查了 biasprm[3]/[4] 的 spec→model→LuGre ODE 通路。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 参数修改器（parameter modifier）
- 工厂函数（factory function）
- 有限差分梯度（finite-difference gradient）
- 刷毛微分方程（LuGre bristle ODE）

## 问题与症状

- MuJoCo sysid 优化 0 次迭代且 LuGre 参数不变时，怎样区分参数通路问题与用户代码错误？
- optimizer 0 iterations、finite-difference gradient 为 0；HTML report 中 initial 与 nominal torque 相同；identified 参数停在 initial。

## 环境

- MuJoCo 3.8.0 Python；Ubuntu 22.04 x86_64；最小单关节 dcmotor/LuGre 模型。

## 优先排查

- 确认传给 sysid.Parameter(modifier=...) 的对象是 callable，而不是 None。
- 在优化前手动改变 param，检查 spec、compile 后 model 和 residual 是否随之变化。

## 可能原因

- 两个 factory 的 return modifier 缩进在 inner modifier 内，outer factory 隐式返回 None。

## 处理过程

- 维护者展开 Python 作用域与缩进，检查 Parameter.apply_modifier 的 falsy no-op 路径。
- 维护者单独验证 biasprm[3] 写入 spec 后能进入 model，且 biasprm[3]/[4] 被 LuGre ODE 读取。

## 有效处理

- 把 return modifier 移到 outer factory 作用域；inner modifier 只更新 spec 参数。

## 结果

- 维护者将问题标为 resolved，并明确不做库侧修改，因为属于用户代码错误。

## 限制

- 原作者没有在评论中贴出修正后的优化数值；结论依据维护者代码诊断与参数通路检查。
- 其他零梯度问题仍可能来自尺度、可辨识性或残差设计，不能一概归因于缩进。

## 安全提示

- 将识别参数用于真实执行器前，应保留边界约束并用独立轨迹验证，不要仅凭优化器退出状态接受参数。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 精确适用于 MuJoCo 3.8.0 sysid 参数 modifier factory 返回 None 的情形；其他零梯度需重新诊断。

## 原始来源

- **来源定位：** MuJoCo #3286 维护者诊断与修正代码 4546762188，库方结论 4549965819
- **问题或回复：** [MuJoCo sysid 零迭代先检查 modifier factory 是否真的返回 callable](<https://github.com/google-deepmind/mujoco/issues/3286#issuecomment-4546762188>)
- **根来源：** [MuJoCo sysid 零迭代先检查 modifier factory 是否真的返回 callable](<https://github.com/google-deepmind/mujoco/issues/3286>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
