<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.isaac-sim.isaaclab.2636.e7ff6545372c.md>)

# 部署 RSL-RL ONNX 时，action_scale 和 clipping 应怎样核对？

> **当前答案：** 原帖回复建议把两层处理拆开核对：ActionTerm 的 scale、clipping 等后处理通常需要在实机侧复现；RSL-RL agent 配置内的 clipping 则可能已经进入导出模型。由于回复带有不确定措辞且没有复测，可靠做法不是直接照搬乘法，而是对同一 observation 比较训练环境 actuator 前的最终 action 与 ONNX 加部署后处理的逐元素结果。

## 可信度与证据状态

- **手册分级：** `L1 草稿或部分经验`
- **分级说明：** 当前只有部分处理经验或排查线索。
- **问题状态：** `partial`
- **证据状态：** `issue_candidate`
- **来源内评级：** 需要实际验证
- **评级理由：** 尚未形成可核对的复现记录。

## 关键术语

- 动作缩放（action scaling）
- 动作裁剪（action clipping）
- 动作项（ActionTerm）
- 模型后处理（post-processing）

## 问题与症状

- RSL-RL 导出的 ONNX 是否已经包含 Isaac Lab ActionTerm 的 action_scale 和 clipping？
- ONNX 输出超过 [-1,1]，例如 1.5 或 -2.0；部署者不确定是否还要乘 action_scale。

## 环境

- Isaac Lab + RSL-RL + ONNX sim-to-real；原帖未给软件版本、机器人或导出脚本提交。

## 优先排查

- 分别列出 RSL-RL agent 配置中的处理与 Isaac Lab ActionTerm 中的 scale/clipping，不要把两层混为一层。
- 用同一 observation 对比训练环境中送入 actuator 前的最终 action 与 ONNX+部署后处理结果。

## 可能原因

- 回复者认为 ActionTerm 后处理可能不在 ONNX 中，而 agent 配置内的 clipping 可能在导出图内。

## 处理过程

- 回复建议在实机侧复现 ActionTerm 后处理，并单独核对 agent 配置是否已包含 clipping。

## 结果

- 得到排查分层，但没有作者复测、导出图检查或目标机器人结果。

## 限制

- 回复使用了 I think/might，不能视为所有版本和导出器的确定契约。
- 不能仅凭 ONNX 输出越界判断 scale 已应用或未应用。

## 安全提示

- 首次上机前离线逐元素对齐 action，并加硬件侧限幅、急停和低增益测试。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- Isaac Lab + RSL-RL 的 ONNX 部署排查入口；版本、导出脚本和 ActionTerm 类型未知，必须实测。

## 原始来源

- **来源定位：** IsaacLab #2636 唯一评论 2954156235
- **问题或回复：** [Isaac Lab 导出 ONNX 后不要假定 ActionTerm 的 scale 与 clipping 已打包](<https://github.com/isaac-sim/IsaacLab/issues/2636#issuecomment-2954156235>)
- **根来源：** [Isaac Lab 导出 ONNX 后不要假定 ActionTerm 的 scale 与 clipping 已打包](<https://github.com/isaac-sim/IsaacLab/issues/2636>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
