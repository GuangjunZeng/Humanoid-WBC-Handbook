<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.coin-or.qpoases.37.bb0d802ab5ad.md>)

# 同一 QP 在 qpOASES C++ 和 Matlab/Simulink 中结果不同，甚至 C++ 报 infeasible 时，应先检查什么？

> **当前答案：** 先逐元素检查 constraint matrix `A` 的存储顺序。维护者说明 C++ 接口期望行主序（row-major），而 Matlab/Simulink 内部采用列主序（column-major）；`A` 被隐式转置时，求解器拿到的是另一个 QP，常表现为不可行。对称 `H` 可能掩盖布局错误。Simulink 还要按接口要求把 `A` 堆叠为向量。只有确认实际传入矩阵一致后，才继续比较求解选项与数值设置。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 行主序（row-major）
- 列主序（column-major）
- 约束矩阵（constraint matrix）
- 不可行二次规划（infeasible quadratic program）

## 问题与症状

- 同一 QP 在 qpOASES C++ 与 Matlab/Simulink 中结果不同或仅 C++ 报 infeasible，先查什么？
- C++ 与 Matlab/Simulink 结果不同；隐式转置 `A` 时常表现为不可行。

## 环境

- qpOASES 3.1.0 的迁移支持记录；C++、Matlab 与 Simulink 接口。

## 优先排查

- 逐元素打印 C++ 实际接收的 `A`，按 row-major 还原并与 Matlab 中的矩阵比较；Simulink 同时检查向量堆叠顺序。

## 可能原因

- 维护者记录的典型原因是 C++ row-major 与 Matlab/Simulink column-major 不匹配，导致 constraint matrix 被隐式转置。

## 处理过程

- 原记录直接给出接口布局差异和 Simulink 的 `A` 向量化要求。

## 有效处理

- 按 C++ row-major 正确构造 `A`；在 Simulink 侧按接口规定的顺序堆叠约束矩阵。

## 结果

- 记录被标记 fixed，但没有附用户的独立复测数据。

## 限制

- 若实际传入矩阵逐元素一致，仍需继续检查 bounds、scaling、options 和精度；不能把所有接口差异都归因于布局。

## 安全提示

- 部署前保存一组黄金 QP，逐元素比较各接口输入与 primal/dual/status，避免静默求解了不同问题。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 直接对应 qpOASES 3.1.0 接口记录；其他版本应以目标接口文档和实际内存数据复核。

## 原始来源

- **来源定位：** qpOASES #37 维护者支持记录正文与 resolution 589978265
- **问题或回复：** [qpOASES C++ 与 Matlab/Simulink 结果不一致先查 A 的存储顺序](<https://github.com/coin-or/qpOASES/issues/37>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
