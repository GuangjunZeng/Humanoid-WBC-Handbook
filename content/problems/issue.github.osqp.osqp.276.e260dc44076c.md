<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.osqp.osqp.276.e260dc44076c.md>)

# OSQP 循环中偶发打满 20000 iterations 时，能否直接归因于 warm start？

> **当前答案：** 不能。维护者对单独数据用默认 setup 在 75 iterations 解出；作者后来披露线上把 `eps_abs/eps_rel` 设为 `1e-8`。维护者建议对 ADMM 使用约 `1e-4` relative tolerance，作者确认有帮助，并补充还必须缩放 condition number 很大的 `P`。原帖没有验证 warm-start 污染，也没有给修正后的最坏时延，因此只能采用“容差＋尺度”的已确认缓解。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 最大迭代（maximum iterations）
- 热启动（warm start）
- 问题缩放（problem scaling）
- 最坏执行时间（worst-case execution time, WCET）

## 问题与症状

- OSQP 在循环中偶发 status 2/-2 并打满迭代时，原线程最终验证了哪些处理？
- 部分周期运行时大幅增加并返回 maximum iterations；单独重放一个数据却 75 iterations 解出。

## 环境

- 2020 OSQP MATLAB 与生成 MEX；实际设置 `eps_abs=eps_rel=1e-8`、`adaptive_rho_interval=25`、`max_iter=20000`。

## 优先排查

- 记录每周期 QP、warm-start state、容差和 `cond(P)`；先对单独数据用默认设置复现，再逐项恢复线上配置。

## 可能原因

- 维护者最终把极严 `1e-8` 容差列为主要原因；作者另确认 `P` 条件数巨大并需要缩放。

## 处理过程

- 放宽 relative tolerance 到约 `1e-4`；缩放问题以处理 `P` 的巨大 condition number。

## 有效处理

- 作者确认两项都有帮助，但没有发布新状态分布或最坏运行时，因此登记为环境内缓解。

## 结果

- 作者回复“does help”，同时说明还必须缩放 `P`；Issue 随后关闭。

## 限制

- 最初关于坏 warm start 的猜测没有被作者确认；没有曲线后的精确尖峰率或实时保证。

## 安全提示

- 必须设置 solver deadline 和 fallback；容差放宽前先定义允许的约束残差，而不是只优化速度。

## 图片与图文证据

- 原帖两张图只显示循环运行时间尖峰与 status 2/-2 的出现位置；图中缺少足够轴信息，未用于推断根因。

## 适用边界

- 适用于原帖极严容差和病态 `P`；其他周期尖峰需保存具体 QP 与 solver state。

## 原始来源

- **来源定位：** OSQP #276 配置 719565150；维护者建议 721814742；作者确认 725366350
- **问题或回复：** [OSQP 周期性 max-iteration 先检查过紧容差和 Hessian 条件数](<https://github.com/osqp/osqp/issues/276#issuecomment-725366350>)
- **根来源：** [OSQP 周期性 max-iteration 先检查过紧容差和 Hessian 条件数](<https://github.com/osqp/osqp/issues/276>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
