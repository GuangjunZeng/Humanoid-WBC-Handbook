<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.stack-of-tasks.pinocchio.1232.18fc1877ef34.md>)

# Pinocchio Python 如何把指定关节固定在参考配置，并在需要显示时同步缩减几何模型？

> **当前答案：** 使用 pinocchio.buildReducedModel，而不是未限定命名空间的 buildReducedModel。待锁对象应通过 model.getJointId 获取 Joint ID；第三个参数 reference_configuration 用来计算锁定关节的放置。只需要运动学/动力学模型时调用 model 重载；还要显示时传入 visual_model，并接收缩减后的 model 与 visual_model。原帖用户确认命名空间修正并分享了工作片段，但其具体参考向量没有经过线程内独立验证。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 缩减模型（reduced model）
- 参考配置（reference configuration）
- 关节索引（joint index）
- 几何模型（GeometryModel）

## 问题与症状

- 如何在 Pinocchio Python 中把完整人形模型的指定关节锁定在给定配置，并保持可视化几何一致？
- 直接调用 buildReducedModel 报 NameError；早期草稿用 getFrameId 收集待锁关节，且不清楚参考配置和几何模型重载。

## 环境

- robotpkg-py36-pinocchio；Python 3.6 路径；RH5 浮动基人形 URDF；RobotWrapper.BuildFromURDF。

## 优先排查

- 确认函数在 pinocchio 命名空间；用 existJointName/getJointId 获取 Joint ID；参考配置必须用于计算锁定关节位姿；需要显示时检查 visual_model 是否同步缩减。

## 可能原因

- 未使用 pinocchio.buildReducedModel 命名空间；混淆 Frame ID 与 Joint ID；未区分只缩减运动学模型和同时缩减几何模型的重载。

## 处理过程

- 维护者贴出 Python docstring；用户按官方 C++ 示例改写 Python 代码并继续修正命名空间和 Joint ID。

## 有效处理

- 调用 pin.buildReducedModel(model, joint_ids, reference_configuration)；需要显示时调用带 geometry_model 的重载并接收 model、geometry_model 两个返回值。

## 结果

- 用户确认命名空间修正正确，并分享其 working Python 片段；两位维护者建议把示例贡献到正式文档。

## 限制

- 用户片段的浮动基前 7 个参考配置全部写为 0，线程没有讨论该四元数是否合法，也没有给出测试输出；不应把整段数值原样视为通用模板。

## 安全提示

- 原始来源未提供独立安全提示；涉及实机时必须先做仿真、限幅和保护验证。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于线程所示 Python 绑定和 RobotWrapper 浮动基模型；目标版本的函数签名仍应通过本地 help/docstring 核对。

## 原始来源

- **来源定位：** Issue #1232，维护者 API 说明 issuecomment-639461045、命名空间说明 issuecomment-640721560、用户工作片段 issuecomment-641755486
- **问题或回复：** [Pinocchio Python 可按参考配置锁定关节，并按需同时缩减几何模型](<https://github.com/stack-of-tasks/pinocchio/issues/1232#issuecomment-641755486>)
- **根来源：** [Pinocchio Python 可按参考配置锁定关节，并按需同时缩减几何模型](<https://github.com/stack-of-tasks/pinocchio/issues/1232>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
