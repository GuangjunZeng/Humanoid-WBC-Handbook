<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.osqp.osqp.205.a444da06e231.md>)

# OSQP MATLAB 与生成的 MEX solver 对同一 QP 迭代行为不同，怎样排除 time-based `rho` 更新差异？

> **当前答案：** 线程中的对照表明，默认 time-based adaptive `rho` 会因两条执行路径的墙钟时间不同而在不同迭代触发。关闭 adaptive rho 后，两者均在 1225 iterations 结束且残差一致；改用固定 `adaptive_rho_interval=25` 后，作者确认原始和简化问题都恢复一致。工程上应固定更新间隔并同步 generated workspace 的 `max_iter`，而不是把 25 当成所有 QP 的最优参数。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 自适应罚参数（adaptive rho）
- 固定更新间隔（fixed update interval）
- 代码生成（code generation）
- 原始/对偶残差（primal/dual residuals）

## 问题与症状

- 为什么 OSQP MATLAB 与同一问题生成的 MEX solver 迭代数和收敛状态不同？
- MATLAB 约 350 iterations 解出；codegen 在 1000 达上限，解误差虽约 `1e-6`。

## 环境

- 2019 OSQP MATLAB/codegen；作者提供原始数据、2–3 维简化 QP 和导出代码；未固定 release。

## 优先排查

- 先让两条路径使用同一固定 `adaptive_rho_interval`，比较 primal/dual residual 和迭代数；同时核对 generated workspace 的 `max_iter`。

## 可能原因

- 维护者实测确认默认 time-based rho update 在两条路径的触发点不同。

## 处理过程

- 关闭 adaptive rho 时，两 solver 都在 1225 iterations 结束且残差一致；随后使用固定更新间隔。

## 有效处理

- 把 `adaptive_rho_interval` 设置为非零固定值，例如线程中验证的 25；不要用墙钟触发做可复现对照。

## 结果

- 原作者确认 `adaptive_rho_interval=25` 对原始与简化问题均有效。

## 限制

- 25 不是所有 QP 的最优 interval；该卡只确认固定 interval 消除了两路径触发差异。

## 安全提示

- 生成代码部署前应固定全部 solver settings，并对最坏迭代数和返回码做硬实时验收。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于 OSQP MATLAB/codegen 的自适应 rho 跨路径复现；其他接口需核对更新策略。

## 原始来源

- **来源定位：** OSQP #205 维护者 A/B 533609939；作者确认 534015433
- **问题或回复：** [OSQP MATLAB 与 codegen 对照应固定 adaptive_rho_interval](<https://github.com/osqp/osqp/issues/205#issuecomment-534015433>)
- **根来源：** [OSQP MATLAB 与 codegen 对照应固定 adaptive_rho_interval](<https://github.com/osqp/osqp/issues/205>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
