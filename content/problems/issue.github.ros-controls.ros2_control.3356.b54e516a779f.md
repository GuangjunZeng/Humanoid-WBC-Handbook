<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.ros-controls.ros2_control.3356.b54e516a779f.md>)

# controller_manager 零 statistics/introspection 订阅者时仍占用明显 CPU，应怎样确认开销来自 pal_statistics worker？

> **当前答案：** 原作者依次排除 executor、controller、RT update thread 和 RMW，再在 overlay 中禁用两个 registry 的初始化、worker 启动与每周期 async publish；两个热线程消失，进程 CPU 约从 13% 降到 1%。这证明了该环境中的开销定位，但没有维护者确认或上游修复。作者 patch 会完全删除观测 topic，不能当作生产修复；subscriber-count gate 也仍只是建议。

## 可信度与证据状态

- **手册分级：** `L1 草稿或部分经验`
- **分级说明：** 当前只有部分处理经验或排查线索。
- **问题状态：** `partial`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 空闲开销（idle overhead）
- 上下文切换（context switch）
- 内省发布器（introspection publisher）
- 订阅者门控（subscriber-count gating）

## 问题与症状

- 为什么 controller_manager 在两个 statistics/introspection topic 都零订阅者时仍有约 13% 单核空闲开销？
- 两个普通优先级 worker 各约 5–7% CPU、约 2000 context switches/s；RT update thread 约 0.1%。

## 环境

- ROS 2 Jazzy，ros2_control 4.44.0-1noble；Ubuntu Noble 6.8；Docker x86_64；CycloneDDS 与 Fast DDS；master 2026-05-27 也复现。

## 优先排查

- 检查 topic subscription count、`ps -T`、线程调度类/CPU/wchan；分别切换 executor、controllers、RMW，再用 overlay 禁用 registry/publish sites 做 A/B。

## 可能原因

- 作者的行为 A/B 将开销定位到两个 pal_statistics registry worker 的每周期唤醒/交接路径；没有维护者确认更细的内部根因。

## 处理过程

- SingleThreadedExecutor、停用 controllers、DDS 调优均无变化；`#if 0` 禁用两个 registry 和 async publish sites 后热线程消失。

## 有效处理

- 作者的本地 workaround 会彻底关闭 introspection/statistics topics，不具备上游合入质量；建议的 subscriber-count gate 尚未实现。

## 结果

- overlay A/B 后 `ros2_control_node` 从约 13% 降到约 1%，容器总 CPU 从约 22% 降到约 9%。

## 限制

- 只有原作者复现，Issue 无维护者技术回复或合并 PR；具体 CPU 百分比不能外推到其他主机。
- 禁用 registry 会失去观测能力，subscriber gate 仍只是作者建议。

## 安全提示

- 真机若临时关闭 introspection，必须保留独立 watchdog、故障状态和关键安全遥测；CPU 优化不能以丢失安全观测为代价。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于该 Jazzy/master + Docker/SBC 类环境的诊断；数值与补丁不可直接外推。

## 原始来源

- **来源定位：** ros2_control #3356 完整 reproducer、线程排除和 overlay A/B；唯一评论为 stale bot
- **问题或回复：** [零订阅者时 pal_statistics worker 仍可能消耗小型主机 CPU](<https://github.com/ros-controls/ros2_control/issues/3356>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
