<!-- Generated by wbc-handbook render-problems; do not edit. -->

[English version](<https://github.com/GuangjunZeng/Humanoid-WBC-Handbook/blob/main/content/problems/en/issue.github.stack-of-tasks.pinocchio.2604.a9365140717d.md>)

# Pinocchio FreeFlyer 模型中 tau 长度等于 model.nv，调用 forwardDynamics 仍报告 q 的 nq 尺寸错误时应检查什么？

> **当前答案：** 先检查调用的算法语义和 Python overload。原帖是无接触前向动力学，应使用 aba(model,data,q,v,tau)；forwardDynamics 面向带接触/约束的动力学重载，需要 constraint_jacobian 和 constraint_drift。原作者的五参数调用匹配了另一重载，导致 q 被解释成应为 nv 长度的 tau，于是出现 got nq=15 的迷惑报错。原作者改用建议后确认解决。

## 可信度与证据状态

- **手册分级：** `L2 来源报告已解决，待独立核验`
- **分级说明：** 原来源报告问题已解决，但尚不等于跨机器人、跨版本通用结论。
- **问题状态：** `resolved`
- **证据状态：** `issue_candidate`
- **来源内评级：** 值得参考
- **评级理由：** 环境、处理步骤和结果已有工程记录，但尚缺正式资料交叉核验或独立复现。

## 关键术语

- 关节空间惯量算法（Articulated-Body Algorithm, ABA）
- 前向动力学（forward dynamics）
- 约束动力学（constrained dynamics）
- 函数重载（function overload）

## 问题与症状

- tau 已按 model.nv 创建，为什么 pin.forwardDynamics 仍报告 expected 10, got 15，且错误指向 tau.size？
- 调用 pin.forwardDynamics(model,data,q,v,tau) 报 ValueError: wrong argument size: expected 10, got 15，尽管 tau 长度为 10。

## 环境

- Pinocchio Python；iris.urdf；JointModelFreeFlyer；model.nq=15、model.nv=10；无 contacts。

## 优先排查

- 使用 help(pinocchio.forwardDynamics) 检查 Python overload 的参数列表。
- 先判断问题是无接触前向动力学，还是带 constraint_jacobian/constraint_drift 的 contact/constrained dynamics。

## 可能原因

- 五参数调用匹配了 constrained forwardDynamics 的另一重载，q 被当成只应为 nv 长度的 tau，因此错误信息显示 got nq=15。

## 处理过程

- 维护者要求完整 URDF 后，与另一位项目成员复核了模型和 API overload。

## 有效处理

- 无接触场景改用 pin.aba(model, data, q, v, tau)；需要接触约束时才按 forwardDynamics 文档提供 constraint_jacobian 与 constraint_drift。

## 结果

- 原作者回复问题已解决。

## 限制

- 该结论只覆盖原帖无接触场景；存在闭链、刚性接触或其他约束时不能直接丢弃 constraint inputs。
- 原帖没有给 Pinocchio 版本和最终加速度数值，只确认 API 选择解决尺寸错误。

## 安全提示

- 实机控制前需确认动力学模型、外力/接触约束与执行器输入定义，不能仅以函数成功返回作为动力学正确性证明。

## 图片与图文证据

- 原始来源未提供可核验图片，或当前采集未包含可分析的图文证据。

## 适用边界

- 适用于无 contact/constraint 的 Pinocchio FreeFlyer forward dynamics；带约束问题必须使用对应 constrained API。

## 原始来源

- **来源定位：** Pinocchio #2604：贡献者评论 2694376878 解释 aba 与 forwardDynamics overload；原作者 2696551706 确认解决
- **问题或回复：** [Pinocchio 无接触前向动力学应调用 aba，forwardDynamics 是约束动力学重载](<https://github.com/stack-of-tasks/pinocchio/issues/2604#issuecomment-2694376878>)
- **根来源：** [Pinocchio 无接触前向动力学应调用 aba，forwardDynamics 是约束动力学重载](<https://github.com/stack-of-tasks/pinocchio/issues/2604>)

> 社区和 Issue 经验用于缩短排查路径；未经论文、源码、官方文档或独立复现核验时，不应直接升级为跨平台工程结论。
